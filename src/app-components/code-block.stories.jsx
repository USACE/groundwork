import { CodeBlock } from "./code-block";

const meta = {
  component: CodeBlock,
};

export default meta;

export const Default = {
  args: {
    code: "This is a preformatted block.\n text should be renderd as-provided.\n",
  },
};
